<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:converters="clr-namespace:FlowChart.Converters"
      xmlns:effects="clr-namespace:FlowChart.Effects"
      x:Class="FlowChart.Views.Controls.NoteListItemView"
      HeightRequest="60">

    <ContentView.Resources>
        <ResourceDictionary>
            <converters:DateTimeToFormattedStringConverter x:Key="dateTimeToFormattedStringConverter" />
        </ResourceDictionary>
    </ContentView.Resources>

    <Grid Margin="6,0"
          Padding="6"
          HorizontalOptions="FillAndExpand"
          BackgroundColor="Lime">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.Effects>
            <effects:RoundCornersEffect CornerRadius="6" />
        </Grid.Effects>

        <Label Grid.Column="0" 
               HorizontalTextAlignment="Center"
               VerticalTextAlignment="Center"
               Text="{Binding Date, Converter={StaticResource dateTimeToFormattedStringConverter}, ConverterParameter=dd/MM}" />
        
        <Label Grid.Column="1" 
               Text="{Binding Note}" />
    </Grid>
    
</ContentView>