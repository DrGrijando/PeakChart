<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:converters="clr-namespace:FlowChart.Converters"
      x:Class="FlowChart.Views.Controls.NoteListItemView"
      HeightRequest="60">

    <Grid.Resources>
        <ResourceDictionary>
            <converters:DateTimeToFormattedStringConverter x:Key="dateTimeToFormattedStringConverter" />
        </ResourceDictionary>
    </Grid.Resources>

    <Grid.RowDefinitions>
        <RowDefinition Height="20" />
        <RowDefinition Height="40" />
    </Grid.RowDefinitions>

    <Label Grid.Row="0" 
           Text="{Binding Date, Converter={StaticResource dateTimeToFormattedStringConverter}, ConverterParameter=dd/MM/yyyy}" />
    
    <Label Grid.Row="1" 
           Text="{Binding Note}" />

</Grid>