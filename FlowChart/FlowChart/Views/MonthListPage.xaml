<?xml version="1.0" encoding="utf-8" ?>
<base:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                      xmlns:base="clr-namespace:FlowChart.Views.Base;assembly=FlowChart"
                      xmlns:controls="clr-namespace:FlowChart.Views.Controls"
                      x:Class="FlowChart.Views.MonthListPage">

    <CollectionView x:Name="months" ItemSizingStrategy="MeasureFirstItem"
                    SelectionMode="Single"
                    ItemsSource="{Binding Months}"
                    SelectedItem="{Binding SelectedMonth}">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical"
                               ItemSpacing="6" />
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <controls:MonthListItemView />
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    
</base:BaseContentPage>