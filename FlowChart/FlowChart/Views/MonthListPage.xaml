<?xml version="1.0" encoding="utf-8" ?>
<base:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:base="clr-namespace:FlowChart.Views.Base;assembly=FlowChart"
             x:Class="FlowChart.Views.MonthListPage">

    <CollectionView ItemSizingStrategy="MeasureFirstItem"
                    SelectionMode="Single"
                    ItemsSource="{Binding Months}"
                    SelectedItem="{Binding SelectedMonth}"                    
                    SelectionChangedCommand="{Binding MonthSelectedCommand}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout Orientation="Horizontal">
                    <Label Text="{Binding Id}" />
                    <Label Text="{Binding Month}" />
                    <Label Text="{Binding Year}" />
                    <Label Text="{Binding ReadingCount}" />
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    
</base:BaseContentPage>